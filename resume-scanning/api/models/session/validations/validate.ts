import type { SessionJobRolesFieldValidationContext } from "gadget-server";

/**
 * Validation code for field JobRoles on session
 */
export default async ({api, record, errors, logger, field}: SessionJobRolesFieldValidationContext) => {
  // TODO: implement validation
  // access things like `record.JobRoles` to get the fields's data
  // add to the `errors` with `errors.add('JobRoles', "did not pass the validation")`
};